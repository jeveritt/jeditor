This Java project can be build with ant.
To build the jeditor.jar file go to this directory and type ant.
The jar output is ../target/jeditor.jar.
To run the program you need to install a Java Standard Development Kit (SDK).
Examples of how to run the program are in ../tools.
